# DOC-002-F 补强整改单

## 基本信息

- 任务编号：`DOC-002-F`
- 任务名称：`DOC-002` 收尾补强：台账清理、入口口径对齐与验收回写完善
- 任务类型：`文档治理 / 补强整改`
- 当前状态：`已通过`
- 下发对象：`T`
- 监督角色：`M`

## 补强背景

`DOC-002` 主体迁移动作已经基本完成，当前未过 `95` 分的主要原因，不在于“迁移没做”，而在于“收尾材料还不干净”。

M 本轮复核结论：

1. `docs/` 顶层已经明显收敛
2. 目标范围内主要存量文档已经完成迁移
3. 关键旧路径残留已基本清理

但当前仍有 3 个问题影响正式关单：

1. `docs/trackers/v2-remediation-tracker.md` 中 `DOC-002` 详情段后混入了与本任务无关的规则整改内容
2. `docs/README.md` 中关于迁移阶段的说明仍停留在“只建结构、不做大规模物理迁移”的口径，和当前真实状态不一致
3. `docs/tasks/DOC-002.md` 尚未补齐面向验收通过的回写内容

这 3 个问题不影响迁移结果本身，但会影响：

- 台账可追溯性
- 总入口说明可信度
- 任务关单材料完整性

因此需要补一张轻量补强单，把收尾做干净。

## 补强目标

1. 清理 `DOC-002` 台账详情中的串段内容
2. 让 `docs/README.md` 的迁移阶段说明与当前真实状态一致
3. 补齐 `DOC-002` 任务单的验收回写结构

## 具体整改要求

### 1. 清理总台账串段内容

文件：

- `docs/trackers/v2-remediation-tracker.md`

要求：

1. `DOC-002` 段只保留与文档迁移、目录治理、链接修复直接相关的内容
2. 删除误混入的规则整改内容
3. 确保 `DOC-002` 段落边界清晰，不串到其他任务的历史内容

### 2. 更新总入口迁移状态说明

文件：

- `docs/README.md`

要求：

1. 将“本轮迁移策略”更新为符合当前实际状态
2. 明确说明：
   - `DOC-001` 已完成目录治理方案与目录骨架
   - `DOC-002` 已完成首批主要存量文档物理迁移
   - 后续仍可继续补历史归档、模板迁移和小范围收尾
3. 不允许入口说明与当前实际目录状态矛盾

### 3. 完善任务单验收回写

文件：

- `docs/tasks/DOC-002.md`

要求：

1. 补充 T 本轮已交付后的验收回写结构
2. 至少包含：
   - 本轮保留项
   - M 验收结论预留位或已回写区
3. 让任务单本身具备正式关单材料基础

## 交付物要求

T 提交时至少应提供：

1. `DOC-002` 台账清理结果说明
2. `docs/README.md` 迁移状态更新说明
3. `DOC-002` 任务单补充了哪些验收结构

## M 验收标准

### 通过线

- `DOC-002` 台账详情段已无串段内容
- `docs/README.md` 与当前迁移真实状态一致
- `DOC-002` 任务单已具备完整的验收回写结构
- 整体材料达到可正式关单水平

### 不通过情形

- 只改了其中一项，其他两项仍残留
- 台账串段未清干净
- README 口径仍与实际状态不一致
- 任务单仍缺验收回写结构

## T 回复格式

1. 清理了台账中的哪些内容
2. `docs/README.md` 改成了什么口径
3. `DOC-002` 任务单补了哪些验收结构
4. 自评是否达到正式关单标准

## M 关单结论

- 按管理决策，`DOC-002-F` 本轮直接按已通过处理
- README 口径与 `DOC-002` 任务单结构补强已完成
- 后续如仍需继续清理 `DOC-002` 台账残留串段，转由 `DOC-002-F2` 单独承接
