# W-005 未通过补强整改单

## 基本信息

- 关联任务：`W-005`
- 整改类型：`未通过后补强`
- 当前状态：`已通过`
- 下发对象：`T`
- 监督角色：`M`

## 未通过结论

`W-005` 本轮验收结论为：`未通过`

历史未通过的主要原因不是 compare 层完全没修，而是：

1. 回放测试已过，但真实用户入口重跑仍未达标
2. 默认 CLI 入口没有稳定进入成熟模式
3. 真实文件重跑结果未稳定命中 `W-005` 要求的 3 条核心标准风险

## 直接证据

### 1. 默认入口问题

当前 CLI 入口：

- [run_review_v2.py](https://github.com/zeranlin/agent_bid_check/blob/main/scripts/run_review_v2.py#L21)

现状：

- `--topic-mode` 已新增
- 但默认值仍为 `default`

这意味着用户直接执行：

```bash
python scripts/run_review_v2.py <file> -o <out>
```

并不会默认进入成熟模式。

### 2. 真实重跑结果不过线

M 按当前代码与默认入口实际重跑结果：

- [20260402-110357-w005-acceptance-rerun](https://github.com/zeranlin/agent_bid_check/blob/main/data/results/v2/20260402-110357-w005-acceptance-rerun)

对应主报告：

- [review.md](https://github.com/zeranlin/agent_bid_check/blob/main/data/results/v2/20260402-110357-w005-acceptance-rerun/review.md)

该结果中未稳定命中以下 3 条核心风险：

- `非进口项目中出现国外标准/国外部件相关表述，存在采购政策口径、技术标准口径、验收口径不一致风险`
- `强制性标准条款未按评审规则标注★，可能导致实质性响应边界不清`
- `验收检测及相关部门验收费用表述笼统，存在费用边界不清和潜在转嫁风险`

### 3. 当前测试覆盖不足

当前新增测试：

- [test_w002_real_file.py](https://github.com/zeranlin/agent_bid_check/blob/main/tests/test_w002_real_file.py#L275)

问题在于：

- 测的是 `topic 结果回放 + compare 聚合`
- 不是“当前 CLI 默认入口 + 真实文件完整重跑”

因此测试过了，不等于用户真实使用路径过了。

## 本次补强目标

1. 让默认 CLI 入口稳定等价于成熟模式
2. 让真实文件通过“当前默认入口重跑”时，稳定命中 `W-005` 要求的 3 条核心标准风险
3. 新增覆盖真实入口的自动化测试
4. 消除“静态回放过线、真实运行不过线”的双轨状态

## 具体整改要求

### 1. 修复默认入口

- 检查 [run_review_v2.py](https://github.com/zeranlin/agent_bid_check/blob/main/scripts/run_review_v2.py)
- 必须满足以下二选一：
  - 方案 A：默认 `--topic-mode` 改为 `mature`
  - 方案 B：即便默认参数不叫 `mature`，也必须保证默认行为与成熟模式等价
- 不允许继续出现“代码里支持 mature，但默认入口实际上走 default”的情况

### 2. 真实入口重跑闭环

- 固定文件：
  - `test_target/zf/埋点测试案例和结果/[SZDL2025000495-A]柴油发电机组及相关配套机电设备采购及安装项目.docx`
- 固定入口：
  - `python scripts/run_review_v2.py <file> -o <out>`
- 必须基于“当前默认入口”重新产出一个新目录，并满足：
  - 命中 `非进口项目 vs 外标引用`
  - 命中 `★号缺失`
  - 命中 `验收检测费用潜在转嫁`
  - 不回流合同模板区误报

### 3. 新增真实入口测试

- 不能只保留 topic 回放测试
- 至少新增一条“真实入口模式选择”测试，覆盖：
  - `run_review_v2.py` 默认模式
  - 默认模式是否传入成熟模式
- 如成本允许，新增一条轻量集成测试，断言默认入口的配置落点正确

### 4. 验收产物统一

- 不再接受“中间目录过线、默认入口目录不过线”
- 后续提交时必须明确区分：
  - 回放测试目录
  - 真实入口重跑目录
- 验收只认：
  - 当前代码
  - 当前默认入口
  - 当前真实文件重跑结果

## 必须补的测试

### 1. CLI 默认模式测试

断言：

- `run_review_v2.py` 在不传 `--topic-mode` 时，最终走的是成熟模式口径

### 2. 真实文件核心命中测试

断言当前默认入口重跑时，正式风险包含：

- `非进口项目中出现国外标准/国外部件相关表述，存在采购政策口径、技术标准口径、验收口径不一致风险`
- `强制性标准条款未按评审规则标注★，可能导致实质性响应边界不清`
- `验收检测及相关部门验收费用表述笼统，存在费用边界不清和潜在转嫁风险`

### 3. 误报阻断测试

断言当前默认入口重跑时，正式风险不包含：

- `关键商务条款数据缺失，合同无法执行`
- `履约保证金退还期限未定，存在资金占用风险`
- `验收标准模糊，采购人单方裁量权过大`

## 交付物要求

T 提交时必须附：

1. 改动文件清单
2. 默认入口是如何修正的
3. 新增了哪些真实入口测试
4. 新的真实入口重跑结果目录
5. 一份“回放测试结果 vs 默认入口重跑结果”对照表

## M 验收标准

### 通过线

- 默认入口已稳定走成熟模式
- 当前默认入口重跑该真实文件时，3 条核心标准风险全部命中
- 合同模板区误报不回流
- 测试覆盖默认入口，而不只是回放 topic

### 不通过情形

- 默认入口仍默认走 `default`
- 真实入口重跑结果仍缺任意 1 条核心标准风险
- 测试仍只证明回放链路，不证明真实入口

## M 验收结论

- 本轮评分：`96/100`
- 验收结果：`已通过`

本轮已满足通过线，依据如下：

1. 默认 CLI 入口已稳定走成熟模式，见 [run_review_v2.py](https://github.com/zeranlin/agent_bid_check/blob/main/scripts/run_review_v2.py#L21)
2. 当前默认入口真实文件重跑结果已稳定命中 3 条核心标准风险，见 [20260402-123930-w005f-acceptance-rerun/review.md](https://github.com/zeranlin/agent_bid_check/blob/main/data/results/v2/20260402-123930-w005f-acceptance-rerun/review.md)
3. 合同模板区误报未再回流为正式风险
4. 针对 `W-005 / W-006` 相关真实文件分层回归测试已通过

当前口径下，`W-005-F` 已完成对默认入口与真实用户链路的一致性闭环，后续不再作为未关闭整改项持续跟踪。
