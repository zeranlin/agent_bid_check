# W-009 真实文件复核后误报、重复报与漏报收敛整改任务单

## 基本信息

- 任务编号：`W-009`
- 任务名称：基于真实文件复核结果，收敛正式风险输出并补齐关键漏报
- 任务状态：`已通过`
- 下发对象：`T`
- 监督角色：`M`

## 任务背景

M 基于真实文件回放结果，对本次成熟专题输出的 `27` 条正式风险做了逐条复核，发现当前结果虽然已有一批命中是正确的，但仍存在以下明显问题：

1. 同一证据组被拆成多条近义正式风险，存在重复报
2. 部分风险方向正确，但标题和定性不够准确
3. 部分证据不足或边界不稳的条目，被直接提升为正式风险
4. 明确应命中的显性风险仍存在漏报

本任务不是单纯新增一条规则，而是要对 `真实文件输出质量` 做一次集中收敛，目标是让正式风险结果更接近“客户可直接阅读”的稳定状态。

本次复核基准结果目录：

- [20260408-szdl0330-verify](/Users/linzeran/code/2026-zn/test_getst/data/results/v2/20260408-szdl0330-verify)

重点参考文件：

- [final_review.md](/Users/linzeran/code/2026-zn/test_getst/data/results/v2/20260408-szdl0330-verify/final_review.md)
- [final_output.json](/Users/linzeran/code/2026-zn/test_getst/data/results/v2/20260408-szdl0330-verify/final_output.json)

## 问题定性

这是一个 `真实文件输出质量收敛问题`，同时覆盖：

- 正式风险去重
- 风险标题与问题定性校正
- 弱证据风险降级
- 关键显性风险漏报补齐
- 回归样本与测试闭环补强

## 整改目标

1. 收缩真实文件中的正式风险输出，消除同义重复报
2. 修正风险标题与正文不一致、定性不准的问题
3. 将弱证据或边界不稳条目降为 `待补证` 或移出正式风险
4. 补齐已确认的显性漏报点
5. 为本次整改补齐样本、测试和真实文件回放闭环

## 具体整改要求

### 1. 去重合并正式风险

以下风险必须合并，不得继续以多条近义正式风险并列输出：

- 风险 `6` 和 风险 `7`
  - 问题本质相同：`业绩限定深圳市政府机关 / 特定行政区域`
  - 要求：只保留 `1` 条正式风险

- 风险 `5` 和 风险 `14`
  - 问题本质相同：`检测/相关部门验收费用被笼统计入投标总价`
  - 要求：只保留 `1` 条正式风险

- 风险 `15` 和 风险 `25`
  - 问题本质相同：`履约保证金 12% 超过法定上限`
  - 要求：只保留 `1` 条正式风险

- 风险 `21`、`22`、`23`
  - 问题本质相同：`项目负责人评分项中学历、职称、证书和分值结构设置不合理`
  - 要求：合并为 `1` 条主风险，避免拆碎输出

### 2. 改写不准确风险文案

以下正式风险方向可保留，但标题、问题重心或定性必须改写：

- 风险 `1`
  - 当前问题：重心偏到“标准格式混写/口径不一致”，不够贴合业务
  - 正确口径：应聚焦 `拒绝进口 vs 外标/国外部件引用矛盾风险`

- 风险 `10`
  - 当前问题：容易把“评分小项最高 100 分”误写成“总评分异常”
  - 正确口径：聚焦 `认证项分值权重偏高、与履约直接关联不足、存在倾向性`

- 风险 `11`
  - 当前问题：将“主观裁量空间过大”表达得过满
  - 正确口径：聚焦 `评分描述量化口径不足、存在评审一致性风险`

- 风险 `21`
  - 当前问题：不应再单独保留碎片化表达
  - 正确口径：并入项目负责人主风险统一输出

### 3. 降级或删除弱证据正式风险

以下条目不应继续按当前口径保留为正式风险：

- 风险 `16`
  - 处理要求：降级为 `待补证` 或删除

- 风险 `17`
  - 处理要求：删除或并入风险 `1`
  - 说明：本文件核心问题不是 `GB252 版本可能废止`，而是 `非进口项目中引用外标 BS2869`

- 风险 `18`
  - 处理要求：降级为 `待补证`
  - 说明：不能仅因全文未搜到相关关键词就直接输出正式风险

- 风险 `26`
  - 处理要求：移出正式风险
  - 说明：当前证据体现的是例外口径，并非明确风险

### 4. 补齐明确漏报

必须补齐以下已确认漏报点，并稳定输出为 `正式风险`：

- 规则口径：
  - `不得要求提供赠品、回扣或者与采购无关的其他商品、服务`

- 真实文件原文特征：
  - `承诺在接到采购人通知后，能够在1小时（60分钟）内到达现场处理问题，并额外向采购人值班室赠送台式电脑、打印机各1套的，得100分`

- 输出要求：
  - 必须进入 `formal_risks`
  - 不得再落为 `pending_review_items`
  - 不得继续漏掉

- 建议风险标题：
  - `评分项要求赠送与采购无关物资，违反“不得要求提供赠品、回扣或者与采购无关的其他商品、服务”的规则`

### 5. 调整输出层口径

在最终输出层必须满足以下要求：

1. 同一证据组不得拆成多条近义正式风险
2. 风险标题、问题定性、证据和整改建议必须保持一致
3. 核心业务风险优先，不得被格式性、辅助性问题喧宾夺主
4. 不得将弱证据条目强行提升为正式风险

### 6. 补齐样本与测试闭环

至少新增或补强以下样本与测试类型：

- 非进口项目 + 外标引用 + 国外部件表述
- 评分项中赠送非项目物资
- 检测/相关部门验收费用打包入总价
- 限定深圳市政府机关业绩
- 项目负责人学历/职称/PMP 组合评分
- 重复报合并与去重测试

要求：

- 新增回归样本
- 新增或修改评测/单元测试
- 用同一真实文件完成复跑对比

## 交付物要求

T 提交时必须附：

1. 改动文件清单
2. 去重、改写、降级、漏报补齐的实现说明
3. 新增样本与测试清单
4. 同一真实文件复跑结果目录
5. 修复前后差异说明

## M 验收标准

### 通过线

- 风险 `6/7`、`5/14`、`15/25`、`21/22/23` 已完成合并收敛
- 风险 `1`、`10`、`11` 的文案已按复核口径改正
- 风险 `16`、`17`、`18`、`26` 不再以当前形式进入正式风险
- “赠送台式电脑、打印机各 1 套”风险已稳定命中
- 同一真实文件复跑后，正式风险数量明显收缩，重复项消失

### 不通过情形

- 仍存在重复正式风险
- 仍保留弱证据正式风险
- “赠送非项目物资”风险继续漏报
- 仅改文案但未补样本/测试/复跑闭环

## M 验收结论

- 验收结果：`已通过`
- 验收评分：`96/100`
- 验收结论：
  - 已完成真实文件中的重复正式风险收敛
  - 已完成关键风险标题与问题定性改写
  - 已将弱证据项按要求降级或移出正式风险
  - 已补齐“赠送台式电脑、打印机”显性漏报，并形成真实文件回放测试闭环
  - 当前剩余事项主要是治理层架构化收口，已转入 `Task-OG1 ~ Task-OG3` 持续推进，不影响 `W-009` 本单通过

## 发给 T 的指令文本

```text
请处理整改任务 W-009：基于真实文件复核结果，收敛正式风险输出并补齐关键漏报。

目标：
1. 合并重复正式风险
2. 改写不准确风险文案
3. 将弱证据风险降级或删除
4. 补齐“赠送非项目物资”明确漏报
5. 补齐样本、测试和真实文件回放闭环

具体要求：

一、去重合并
- 合并风险6和7：深圳市政府机关业绩限定，只保留1条正式风险
- 合并风险5和14：检测/相关部门验收费用打包入总价，只保留1条正式风险
- 合并风险15和25：履约保证金12%超标，只保留1条正式风险
- 合并风险21/22/23：项目负责人评分项合并为1条主风险

二、文案改写
- 风险1改成“拒绝进口 vs 外标/国外部件引用矛盾风险”
- 风险10不要再用“小项100分=总分异常”口径，改成认证项权重偏高、与履约关联不足
- 风险11改成“量化口径不足、存在评审一致性风险”
- 项目负责人相关风险统一到合并后的主风险表达

三、降级或删除
- 风险16降为待补证或删除
- 风险17删除或并入风险1
- 风险18降为待补证
- 风险26移出正式风险

四、补齐漏报
- 必须补中“赠送台式电脑、打印机各1套”的正式风险
- 规则口径：不得要求提供赠品、回扣或者与采购无关的其他商品、服务
- 输出层级：正式风险

五、补测试
- 增加上述场景的回归样本
- 增加重复报合并测试

完成后提交：
1. 改动文件清单
2. 新增/修改的规则、样本、测试说明
3. 用同一真实文件复跑后的结果对比
4. 说明哪些旧风险被合并、删除、降级
```
