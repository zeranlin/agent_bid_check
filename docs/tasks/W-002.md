# W-002 整改任务单

## 基本信息

- 任务编号：`W-002`
- 任务名称：补齐真实文件中 R-001~R-007 标准命中映射、compare 规则码与 Web 展示一致性闭环
- 任务状态：`待下发`
- 下发对象：`T`
- 监督角色：`M`

## 任务背景

基于真实历史结果：

- [review.md](/Users/linzeran/code/2026-zn/test_getst/data/results/v2/20260401-124857-763f549f/review.md)
- [comparison.json](/Users/linzeran/code/2026-zn/test_getst/data/results/v2/20260401-124857-763f549f/comparison.json)
- [scoring.json](/Users/linzeran/code/2026-zn/test_getst/data/results/v2/20260401-124857-763f549f/topic_reviews/scoring.json)
- [acceptance.json](/Users/linzeran/code/2026-zn/test_getst/data/results/v2/20260401-124857-763f549f/topic_reviews/acceptance.json)

复核发现当前 Web / compare 链路存在“语义命中但未沉淀为标准任务口径”的问题。

## 本次真实文件核查结论

### R-001

- 现状：`已标准命中`
- 证据：`comparison.metadata.failure_reason_codes = ['policy_technical_inconsistency']`
- 结论：保持现状，不得回退

### R-002

- 现状：`有真实场景，但未标准命中`
- 证据：正文存在 `（7）燃油标准，采用 0 号轻柴油，符合 GB252 或 BS2869 标准。`
- 当前缺口：未按“强制性标准条款未按评审规则标注★”标准口径打出

### R-003

- 现状：`有真实场景，但未标准命中`
- 证据：正文存在 `安装、检测、验收、培训计划` 评分项
- 当前缺口：review 仅打出“评分逻辑错误”，未按“不得将项目验收方案作为评审因素”标准口径落出

### R-004

- 现状：`本文件无对应场景`
- 要求：保持不误报

### R-005

- 现状：`本文件无对应场景`
- 要求：保持不误报

### R-006

- 现状：`语义命中，但未标准命中`
- 证据：已出现：
  - `制造商资质证书评分项指向特定认证，具有排他性`
  - `产品认证指定‘省级标准协会’，存在地域或特定机构排斥风险`
- 当前缺口：未落到标准规则码 `specific_brand_or_supplier_in_scoring_forbidden`

### R-007

- 现状：`语义命中，但未标准命中`
- 证据：已出现：
  - `将验收产生的检测费用及相关部门验收费用笼统计入投标人承担范围，存在需求条款合规风险`
- 当前缺口：未稳定落到标准 compare 规则码 `acceptance_testing_cost_shifted_to_bidder`

## 问题定位

当前真实文件链路存在以下问题：

1. 专题侧已抽到正文信号，但规则侧信号未稳定并入同次 compare
2. scoring 专题召回章节存在偏移，未稳定命中真实评分规则主章节
3. acceptance 专题抽到正文承担范围信号，但规则侧禁令未稳定带入
4. 最终 Web 页面更偏向 baseline 泛化标题，没有把整改后的标准任务口径前置展示

## 整改目标

让真实文件运行结果对 `R-001 ~ R-007` 达到以下状态：

1. `R-001` 标准命中并保持稳定
2. `R-002` 标准命中
3. `R-003` 标准命中
4. `R-004` 不误报
5. `R-005` 不误报
6. `R-006` 标准命中
7. `R-007` 标准命中

## 具体整改要求

### 1. 真实文件回放整改

必须以真实历史文件 `20260401-124857-763f549f` 作为回放样本。

需要补一套“真实文件命中矩阵”校验，逐项验证：

- 是否存在场景
- 是否抽到规则侧信号
- 是否抽到正文侧信号
- 是否生成标准 compare 规则码
- Web 是否展示出标准任务口径

### 2. 规则侧召回 / 注入整改

修复真实文件链路中规则侧 structured signals 未稳定注入 compare 的问题，至少包括：

- `R-002` 星标规则侧信号
- `R-003` 验收方案评分禁入规则侧信号
- `R-006` 特定认证/制造商限定规则侧信号
- `R-007` 验收检测费用禁转嫁规则侧信号

### 3. compare 合并整改

确保 compare 在真实文件下能够同时拿到：

- 规则侧信号
- 正文侧信号
- 标准标题
- 标准 failure_reason_codes

至少需要稳定产出：

- `policy_technical_inconsistency`
- `star_marker_missing_for_mandatory_standard`
- `acceptance_plan_in_scoring_forbidden`
- `specific_brand_or_supplier_in_scoring_forbidden`
- `acceptance_testing_cost_shifted_to_bidder`

说明：

- `R-004`、`R-005` 在该真实文件中不应产出规则码

### 4. Web 展示整改

Web 历史页中：

- 优先展示整改后的标准风险标题
- 不得只剩 baseline 泛化标题而看不到标准任务口径
- `review.md`、`comparison.json`、Web 页面三者口径需一致

## 必须补的测试

### 1. 真实文件回放测试

至少新增一组针对真实文件结果的回放测试，断言：

- `R-001` 命中
- `R-002` 命中
- `R-003` 命中
- `R-004` 不命中
- `R-005` 不命中
- `R-006` 命中
- `R-007` 命中

### 2. compare 规则码测试

断言真实文件下 `comparison.json` 中出现对应规则码，而不是只在 baseline 风险标题里“语义类似”。

### 3. Web 一致性测试

断言最终展示结果与 `comparison.json` 中的标准标题一致。

## 交付物要求

T 提交时必须附：

1. 一份真实文件 `R-001 ~ R-007` 命中矩阵
2. 改了哪些文件
3. 修了哪些规则侧召回 / 注入 / compare 合并逻辑
4. 新增了哪些真实文件回放测试
5. 跑了哪些命令，结果如何
6. 一份新的真实结果目录
7. 对应 Web 历史页链接

## M 验收标准

### 通过线

- 真实文件结果中，`R-001 / R-002 / R-003 / R-006 / R-007` 按标准任务口径可见
- `R-004 / R-005` 在该文件中保持不误报
- `comparison.json` 中出现对应标准规则码
- Web 页、review.md、comparison.json 三者口径一致

### 不过线情形

- 只在 baseline 泛化标题中出现，未映射到标准任务口径
- structured signals 有正文信号，但 compare 仍缺规则码
- Web 展示与 comparison.json 不一致
- 把 `R-004 / R-005` 在该文件中误报出来

## T 回复格式

请按以下格式回复：

1. 本次修的是哪条真实链路缺口
2. 改了哪些文件
3. 修了哪些规则侧召回 / compare 合并 / Web 展示逻辑
4. 新增了哪些真实文件测试
5. 跑了哪些命令，结果如何
6. 新结果目录是什么
7. Web 新链接是什么
8. `R-001 ~ R-007` 最终命中矩阵
9. 自评是否已过线
