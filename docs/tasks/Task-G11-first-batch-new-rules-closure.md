# Task-G11 首批新规则统一收口任务单

## 基本信息

- 任务编号：`Task-G11`
- 任务名称：首批新规则统一收口
- 任务类型：`治理架构实施`
- 当前状态：`已通过`
- 下发对象：`T`
- 监督角色：`M`

## 任务背景

`R-009 ~ R-012` 已完成首批真实候选正式纳管的开发落地，但当前仍存在“功能已补齐、治理口径未完全收口”的问题：

1. 部分任务单状态未按验收结论回写
2. 部分规则注册表仍停留在 `review`
3. 总台账尚未完整体现 `R-009 ~ R-012` 的当前状态和结论
4. 缺少统一的真实命中输出示例归档和收口说明

## 任务目标

1. 统一回写 `R-009 ~ R-012` 的任务单状态
2. 统一回写 `R-009 ~ R-012` 的注册表状态
3. 在总台账中补齐首批新规则的状态与结论
4. 为每条规则补一份命中输出示例归档
5. 输出一份首批新规则纳管收口说明

## 本轮覆盖规则

1. `R-009`
2. `R-010`
3. `R-011`
4. `R-012`

## 本轮执行口径

1. 严格按现有 `M` 验收结论回写状态
2. `R-009 ~ R-011` 已有明确通过结论，按 `已通过 / active` 收口
3. `R-012` 当前仅有开发完成与测试通过证据，暂无 `M` 正式通过结论，按 `待验收 / in_progress` 收口
4. 本轮不继续新增新规则能力

## 必须交付物

1. 更新后的 `R-009 ~ R-012` 任务单
2. 更新后的 `R-009 ~ R-012` 规则注册表
3. 更新后的总台账
4. 首批新规则命中输出示例归档
5. 首批新规则纳管收口说明

## 验收关注点

1. 任务单、注册表、台账三处状态口径一致
2. `R-009 ~ R-012` 规则引用仍有效
3. 每条规则都有命中输出示例归档
4. `R-012` 未越过 `M` 现有验收边界

## M 验收结论

- 验收结果：`已通过`
- 验收评分：`96/100`
- 验收结论：
  - 已完成 `R-009 ~ R-012` 的任务单状态回写、规则注册表激活、总台账补齐、命中输出示例归档与收口说明整理
  - 当前三处核心治理对象已基本一致，满足首批新规则统一收口目标
  - `Task-G11` 可按通过口径收口，不再作为持续整改项单独跟踪
