# V2 整改任务总台账

## 1. 说明

本台账用于统一管理 V2 阶段的整改任务，避免后续在任务下发、回传、验收、关单过程中出现编号混乱、状态不清和结论不可追溯的问题。

后续所有整改任务，统一按“任务编号 + 名称 + 状态 + 验收结论”方式管理。

## 2. 任务编号规则

- `R`：规则与风险识别整改
- `E`：样本、评测、监督标准整改
- `W`：Web、链路、展示与运行整改

## 3. 状态定义

- `待下发`：任务已明确，但尚未正式交付给 T
- `整改中`：T 已开始整改，尚未提交验收
- `待验收`：T 已回传结果，等待 M 复核
- `已通过`：M 已完成验收并判定通过
- `未通过`：M 已验收但未过线，需要继续整改
- `已关闭`：任务通过验收，且确认不再继续跟进

## 4. 整改任务总表

| 任务编号 | 任务名称 | 类型 | 当前状态 | 当前结论 |
| --- | --- | --- | --- | --- |
| R-001 | 补齐“拒绝进口 vs 外标引用”跨专题一致性风险识别 | 规则整改 | 已通过 | 已完成并通过验收 |
| R-002 | 补齐“强制性标准条款未按评审规则加注★”风险识别 | 规则整改 | 已关闭 | 已完成验收并正式关单 |
| R-003 | 补齐“不得将项目验收方案作为评审因素”评分合规风险识别 | 规则整改 | 已关闭 | 已完成验收并正式关单 |
| R-004 | 补齐“不得将付款方式作为评审因素”评分合规风险识别 | 规则整改 | 待下发 | 尚未进入验收 |
| E-001 | 补齐“拒绝进口 vs 外标引用”相关样本与回归测试 | 样本/评测整改 | 已通过 | 已为 R-001 提供支撑 |
| E-002 | 补齐“二层 technical_standard 召回与去噪”监督样本 | 样本/评测整改 | 已通过 | 已为 R-001 提供支撑 |
| W-001 | review-plus Web 链路重启与新结果复核 | Web/运行链路整改 | 已通过 | Web 新结果已与修复后规则对齐 |

## 5. 任务详情

### R-001

- 任务名称：补齐“拒绝进口 vs 外标引用”跨专题一致性风险识别
- 任务类型：规则整改
- 当前状态：`已通过`
- 当前结论：已完成并通过验收

#### 风险来源

- 文件中存在明确拒绝进口表述
- 同时存在外标引用，如 `BS EN 61000`、`EN55011`
- 系统此前未能稳定识别二者之间的跨专题冲突

#### M 验收结论

- 已能识别 `不接受投标人选用进口产品`
- 已能识别 `BS EN 61000 / EN55011 / GB/T 17626`
- 已触发 `policy_technical_inconsistency`
- Web 最新结果已命中该风险

#### 验收参考

- [review.md](/Users/linzeran/code/2026-zn/test_getst/data/results/v2/20260401-093548-1e454417/review.md)
- [comparison.json](/Users/linzeran/code/2026-zn/test_getst/data/results/v2/20260401-093548-1e454417/comparison.json)
- [technical_standard.json](/Users/linzeran/code/2026-zn/test_getst/data/results/v2/20260401-093548-1e454417/topic_reviews/technical_standard.json)

### R-002

- 任务名称：补齐“强制性标准条款未按评审规则加注★”风险识别
- 任务类型：规则整改
- 当前状态：`已关闭`
- 当前结论：已完成验收并正式关单

#### 风险来源

- 规则要求：`含有GB（不含GB/T）或国家强制性标准的描述中需含有★号`
- 业务条款示例：`符合 GB252 或 BS2869 标准`
- 正文未加 `★`

#### 整改目标

- 识别 `GB` 非 `GB/T`
- 识别“规则要求必须加 `★`”
- 对齐正文未加 `★` 的情形并报风险

#### 标准风险文案

- 风险标题：`强制性标准条款未按评审规则标注★，可能导致实质性响应边界不清`
- 问题定性：`中风险`
- 审查类型：`评审规则一致性 / 实质性条款标识完整性`

#### 风险判断要求

- 评审规则已明确：含 `GB`（不含 `GB/T`）或国家强制性标准的描述，应标注 `★`
- 当前条款引用了 `GB252` 等 `GB` 非 `GB/T` 标准，但正文未加 `★`
- 可能导致投标人无法准确识别其是否属于实质性条款
- 若评审阶段按实质性条款处理，存在废标争议和评审口径不一致风险

#### 整改建议要求

- 若该条款属于实质性要求，应在条款前明确加注 `★`
- 若不作为实质性条款，应同步修改评审规则或补充解释，保持规则与正文一致

#### 规则整改要求

1. 新增评审规则侧信号提取
2. 识别下列语义：
   - `含有GB（不含GB/T）`
   - `国家强制性标准`
   - `需含有★号`
   - `应标注★`
   - `实质性条款需加★`
3. 建议形成结构化信号：
   - `star_required_for_gb_non_t = true`
   - `star_required_for_mandatory_standard = true`

#### 正文识别要求

1. 识别正文技术条款中：
   - 是否命中 `GB` 但不是 `GB/T`
   - 是否命中“国家强制性标准”语义
   - 条款标题或条款正文前是否已有 `★`
2. 建议形成结构化信号：
   - `contains_gb_non_t = true`
   - `contains_gbt = false`
   - `contains_mandatory_standard = false/true`
   - `has_star_marker = false`

#### Cross-check 要求

当同时满足：

- 规则要求该类条款必须加 `★`
- 正文条款命中 `GB`（非 `GB/T`）或国家强制性标准
- 正文条款未加 `★`

则输出该风险点。

#### 样本整改要求

1. 正样本
   - `符合 GB252 标准`，未加 `★`
   - `符合 GB 50016 标准`，未加 `★`
   - `符合国家强制性标准`，未加 `★`
   - `符合 GBxxx 或外标`，未加 `★`
2. 负样本
   - `符合 GB/T 17626 标准`，未加 `★`，不应触发
   - `★符合 GB252 标准`，已加 `★`，不应触发
   - 规则未要求 `GB` 条款必须加 `★` 的项目，不应触发
   - 普通技术参数条款，不含 `GB`、不含强制标准语义，不应触发
3. 真实业务表达样本
   - `（7）燃油标准，采用0号轻柴油，符合 GB252 或 BS2869 标准。`
   - 配套规则：`评审规则合理性-含有GB（不含GB/T）或国家强制性标准的描述中需含有★号。`

#### 测试整改要求

1. 规则抽取测试
2. 正文识别测试
3. 端到端测试

#### 交付物要求

1. 改了哪些文件
2. 新增了哪些结构化信号
3. Cross-check 规则怎么设计
4. 新增了哪些样本
5. 新增了哪些测试
6. 测试运行结果
7. 至少一份端到端输出结果，证明该风险点已能被打出

#### M 验收标准

通过线：

- 能稳定识别 `GB`（非 `GB/T`）+ 未加 `★` 风险
- 不把 `GB/T` 误报进去
- 端到端能打出标准风险文案
- 回归测试完整

不过线情形：

- 只识别 `GB`，但不会判断 `GB/T`
- 只会看正文，不会对齐评审规则
- 误把所有 `GB/T` 都报成缺 `★`
- 风险文案不清楚，无法直接给业务使用

#### T 回复格式

1. 本次补的是哪类风险
2. 改了哪些文件
3. 新增了哪些规则与结构化字段
4. 新增了哪些样本与测试
5. 跑了哪些命令，结果如何
6. 给出一份命中该风险的实际输出示例
7. 自评是否已过线

#### M 关单结论

- 已完成规则抽取、正文识别、cross-check 与样本测试闭环
- 已能区分 `GB` 与 `GB/T`
- 已能识别“未加 ★”的实质性条款标识缺失风险
- 当前已满足关单条件，后续不再作为未关闭整改项跟踪

### R-003

- 任务名称：补齐“不得将项目验收方案作为评审因素”评分合规风险识别
- 任务类型：规则整改
- 当前状态：`已关闭`
- 当前结论：已完成验收并正式关单

#### 风险来源

- 新规则：`不得将项目验收方案作为评审因素`
- 当前评分项包含：
  - `项目验收移交衔接方案`
  - `项目验收资料编制与移交衔接安排`
- 且与分值直接挂钩

#### 整改目标

- 识别规则禁令
- 识别评分项中的验收方案内容
- 判断是否与得分挂钩
- 输出标准风险文案

#### 标准风险文案

- 风险标题：`将项目验收方案纳入评审因素，违反评审规则合规性要求`
- 问题定性：`中高风险`
- 审查类型：`评分因素合规性 / 评审规则设置合法性`

#### 风险判断要求

- 评审规则已明确不得将项目验收方案作为评审因素
- 当前评分内容中纳入了验收移交方案或验收资料移交安排
- 相关内容与评分标准、得分或加分直接关联
- 存在评分因素设置不合规、评审争议和中标结果不稳风险

#### 整改建议要求

- 将验收方案、验收资料移交安排从评分因素中删除
- 如确需提出要求，应调整至履约、实施或验收管理条款，不得作为评分项
- 对评分标准重新拆分，仅保留允许纳入评分的实施能力内容

#### 规则整改要求

1. 新增规则侧信号提取
2. 识别以下语义：
   - `不得将项目验收方案作为评审因素`
   - `验收方案不得纳入评分`
   - `验收移交方案不得作为评审项`
   - `验收资料不得作为评分因素`
3. 建议形成结构化字段：
   - `acceptance_plan_forbidden_in_scoring = true`

#### 评分项识别要求

1. 识别评分内容中以下关键词：
   - `验收方案`
   - `项目验收`
   - `验收移交`
   - `移交衔接`
   - `验收资料`
   - `验收安排`
   - `竣工验收方案`
2. 并判断其是否出现在：
   - `评审内容`
   - `评审标准`
   - `评分因素`
   - `得分`
   - `加分`
   - `最高得X分`
3. 建议形成结构化字段：
   - `scoring_contains_acceptance_plan = true`
   - `acceptance_plan_scoring_sections = [...]`
   - `acceptance_plan_scoring_sentences = [...]`

#### Cross-check 要求

当同时满足：

- 规则要求“不得将项目验收方案作为评审因素”
- 评分项中出现验收方案类内容
- 且与打分、得分、分值挂钩

则输出该风险点。

#### 样本整改要求

1. 正样本
   - `项目验收移交衔接方案` 被纳入评审内容
   - `验收资料编制与移交安排` 被纳入评分
   - `验收方案` 与 `得分 / 最高得 / 加分` 同段出现
2. 负样本
   - 验收方案只出现在合同、履约、验收章节，不参与评分
   - 评分项仅评施工组织、安全保障，不含验收内容
   - 文中出现“验收”字样，但不是评分因素
3. 真实业务表达样本
   - `评审内容`
   - `评审标准`
   - `最高得30分`
   - `项目验收移交衔接方案`
   - `项目验收资料编制与移交衔接安排`
   - `投标人提供的安装、检测、项目验收方案及培训计划`
   - `提供项目验收方案设计、验收标准及验收流程安排`
   - `验收资料准备节点明确、流程衔接合理，能体现项目验收组织能力的，评价为优得60分`

#### R-003 新增业务正样本

以下案例已确认为 `R-003` 的核心正样本，后续验收必须覆盖：

`一、评审内容：投标人提供的安装、检测、项目验收方案及培训计划，包括但不限于：提供安装计划安排；提供检测计划安排；提供项目验收方案设计、验收标准及验收流程安排；提供培训计划安排；二、评审标准：方案每体现1点加10分，最高得40分，再此基础上安装、检测、验收衔接计划条理清晰，验收资料准备节点明确、流程衔接合理，能体现项目验收组织能力的，评价为优得60分；2.安装、检测、验收、培训计划条理较清晰，脉络较分明，操作性较强，能体现以人为本、服务至上的售后理念，评价为良得30分；3.安装、检测、验收、培训计划条理模糊，脉络较分明，操作性较强，不能能体现以人为本、服务至上的售后理念，评价为中得10分；4、安装、检测、验收、培训计划条理模糊，脉络不分明，操作性差，不能能体现以人为本、服务至上的售后理念，评价为差，不得分。`

该样本命中理由：

- 评分项直接纳入了 `项目验收方案`
- 同时纳入了 `验收标准`、`验收流程安排`、`验收资料准备节点`
- 并与 `加分 / 得分 / 评价为优得60分` 直接挂钩
- 按规则应判定为“将项目验收方案纳入评审因素”的中高风险问题

#### 测试整改要求

1. 规则抽取测试
2. 评分项识别测试
3. 端到端测试

#### 交付物要求

1. 改了哪些文件
2. 新增了哪些结构化信号
3. Cross-check 规则怎么实现
4. 新增了哪些样本
5. 新增了哪些测试
6. 测试运行结果
7. 一份实际命中结果示例

#### M 验收标准

通过线：

- 能稳定识别“验收方案进入评分因素”的问题
- 不把普通验收条款误报成评分违规
- 风险文案完整、可直接给业务使用
- 回归测试齐全

不过线情形：

- 只识别“验收”字样，不判断是否进入评分
- 只识别评分，不对齐规则禁令
- 误把履约、合同里的验收条款报成评分风险
- 没有端到端样本和测试

#### T 回复格式

1. 本次补的是哪类风险
2. 改了哪些文件
3. 新增了哪些规则与结构化字段
4. 新增了哪些样本与测试
5. 跑了哪些命令，结果如何
6. 给出一份命中该风险的实际输出示例
7. 自评是否已过线

#### M 关单结论

- 已完成规则抽取、评分项识别、cross-check 与 regression 闭环
- 已覆盖第一版“验收移交 / 验收资料编制”场景
- 已覆盖第二版强业务表达：
  - `项目验收方案设计`
  - `验收标准`
  - `验收流程安排`
  - `验收资料准备节点明确`
  - `项目验收组织能力`
  - `评价为优得60分 / 良得30分 / 中得10分 / 不得分`
- 当前已满足关单条件，后续不再作为未关闭整改项跟踪

### E-001

- 任务名称：补齐“拒绝进口 vs 外标引用”相关样本与回归测试
- 任务类型：样本/评测整改
- 当前状态：`已通过`
- 当前结论：已为 R-001 提供支撑

#### M 验收结论

- 样本已补
- compare / pipeline / regression 相关测试已通过
- 能支持真实文件检出

### R-004

- 任务名称：补齐“不得将付款方式作为评审因素”评分合规风险识别
- 任务类型：规则整改
- 当前状态：`待下发`
- 当前结论：尚未进入验收

#### 风险来源

- 新规则：`评审规则合规性-不得将付款方式作为评审因素`
- 当前评分项包含：
  - `付款周期短于招标文件要求`
  - `预付款比例更有利于采购人资金安排`
- 且与 `每项加10分，最高加20分` 直接挂钩

#### 整改目标

- 识别规则禁令
- 识别评分项中的付款周期、预付款比例、付款节点、支付安排等内容
- 判断是否与得分、加分、分值挂钩
- 输出标准风险文案

### E-002

- 任务名称：补齐“二层 technical_standard 召回与去噪”监督样本
- 任务类型：样本/评测整改
- 当前状态：`已通过`
- 当前结论：已为 R-001 提供支撑

#### M 验收结论

- 已补“规格及技术参数”优先召回测试
- 已补“source_location / source_excerpt 收敛”测试
- 已验证 targeted replay clean 产物

### W-001

- 任务名称：review-plus Web 链路重启与新结果复核
- 任务类型：Web/运行链路整改
- 当前状态：`已通过`
- 当前结论：Web 新结果已与修复后规则对齐

#### M 验收结论

- 旧历史页是修复前结果，不能作为验收依据
- Web 服务已重启
- 新历史记录已正确命中 R-001 风险

#### 验收参考

- [review.md](/Users/linzeran/code/2026-zn/test_getst/data/results/v2/20260401-093548-1e454417/review.md)

## 6. 当前优先级建议

### P1

- `R-002` 强制性标准条款未按规则加 `★`
- `R-003` 不得将项目验收方案作为评审因素

#### 原因

- 都是业务最新反馈的真实漏检点
- 都属于“规则已明确，但系统未覆盖”的典型问题
- 都可能继续在真实项目中重复出现

### P2

- 政策专题不必要的 `manual_review` 降级继续收敛
- 风险证据摘录进一步压缩
- Web 历史页展示一致性继续优化

## 7. 后续使用建议

后续所有任务建议统一按以下格式下发与回传：

1. 任务编号
2. 任务名称
3. 风险来源
4. 整改目标
5. 交付物要求
6. M 验收标准
7. 当前状态
8. 验收结论

后续对账时，优先直接使用任务编号沟通，例如：

- `评估 R-002`
- `给 T 下发 R-003`
- `汇总当前所有未关闭任务`
- `R-001 是否可以正式关单`

## 8. 待维护项

本台账当前为 v1。后续每新增一项整改任务，需同步补充：

- 总表
- 任务详情
- 当前状态
- M 的最新验收结论
