# Rule Governance

`rules/` 用于落地 V2 的规则治理骨架，承接后续规则模板、注册表、校验器和迁移资产。

- `registry/`：正式规则文件注册表
- `candidates/`：候选规则池，与正式注册表物理隔离
- `templates/`：新规则创建模板
- `schemas/`：结构与状态约束
- `tools/`：规则治理工具说明
- `examples/`：样例与占位说明
